# Install these with:  pip install -r requirements.txt

duckdb>=1.1        # the database - lives in one file, no server to set up
pandas>=2.0        # building the dimension tables, and the number checks
pyarrow>=15.0      # writing Parquet files, which is what Power BI reads
requests>=2.31     # downloading from Statistics Canada and the CBA
pdfplumber>=0.11   # reading the arrears numbers out of the CBA's PDF

pytest>=8.0        # running the tests
